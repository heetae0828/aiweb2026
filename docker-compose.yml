version: "3.9"
services:
  walrus-study:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./history.json:/app/history.json
